# Gemma endpoint (Ollama OpenAI-compatible)
GEMMA_BASE_URL=http://localhost:11434/v1
GEMMA_MODEL=gemma3:4b
EMBED_MODEL=embeddinggemma:300m
# Serve precomputed outputs for the demo patient when Gemma is unavailable
USE_CACHE=true
