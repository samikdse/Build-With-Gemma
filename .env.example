# Gemma endpoint for the NEXT phase (live integration).
# The current phase runs fully on deterministic fixtures.
VITE_GEMMA_BASE_URL=http://localhost:11434/v1
VITE_GEMMA_MODEL=gemma3:4b
VITE_EMBED_MODEL=embeddinggemma:300m
